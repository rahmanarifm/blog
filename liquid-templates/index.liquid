<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>broadmin.website</title>
    <link rel="stylesheet" href="https://unpkg.com/tachyons@4.10.0/css/tachyons.min.css"/>
  </head>
  <body class="code">
    <article>
      <h2 class="f3 fw4 pa3 mv0 code">changlog of <a href="https:/rm.broadmin.com/" class="link underline-hover gray">/rm.broadmin.com</a></h2>
      <div class="cf pa2">
      {% for screenshot in screenshots %}
        <div class="fl w-100 w-25-m w-25-l mw8 pa2">
          <a href="/posts/{{screenshot.name}}.html" class="db link dim tc">
            <img src="/screenshots/{{screenshot.filename}}" class="w-100 db outline black-10"/>
            <dl class="mt2 f6 lh-copy">
              <dt class="clip">Date</dt>
              <dd class="ml0 black truncate w-100">{{screenshot.date}}</dd>
              <dt class="clip">Year</dt>
              <dd class="ml0 gray truncate w-100">{{screenshot.year}}</dd>
            </dl>
          </a>
        </div>
        {% endfor %}
      </div>
    </article>
  </body>
</html>
